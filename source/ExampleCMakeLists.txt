cmake_minimum_required(VERSION 3.20)

project(example_project LANGUAGES CXX CUDA)

# If nothing displays, try setting this to 75 or to match the architecture number of the library:
# set(CMAKE_CUDA_ARCHITECTURES 75)

#Gets CPM
set(CPM_FILE ${CMAKE_CURRENT_BINARY_DIR}/cmake/CPM.cmake)

if(NOT EXISTS ${CPM_FILE})
  file(MAKE_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/cmake)

  file(
    DOWNLOAD
    https://github.com/cpm-cmake/CPM.cmake/releases/download/v0.42.0/CPM.cmake
    ${CMAKE_CURRENT_BINARY_DIR}/cmake/CPM.cmake
  )
endif()

include(${CPM_FILE})



# Gets SDL2
CPMAddPackage(
  NAME SDL2
  URL https://github.com/libsdl-org/SDL/archive/refs/tags/release-2.30.6.zip
)

# Gets glfw3
CPMAddPackage(CPM_FILE
  NAME glfw
  URL https://github.com/glfw/glfw/releases/download/3.4/glfw-3.4.zip
)

# Gets GLEW
CPMAddPackage(CPM_FILE
  NAME GLEW
  URL https://github.com/nigels-com/glew/releases/download/glew-2.2.0/glew-2.2.0.zip
)



find_package(scve REQUIRED)

add_executable(test your_cuda_file.cu)

set_source_files_properties(test PROPERTIES LANGUAGE CUDA)

target_link_libraries(test PRIVATE scve::scve)